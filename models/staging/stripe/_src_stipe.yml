version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        config:
          freshness: 
            error_after:
              count: 5
              period: day
            warn_after:
              count: 1
              period: day
          loaded_at_field: _batched_at
        